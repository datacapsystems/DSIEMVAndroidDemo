plugins {
    // Lets Gradle auto-download a JDK matching gradle/gradle-daemon-jvm.properties
    // when none is installed locally.
    id 'org.gradle.toolchains.foojay-resolver-convention' version '0.10.0'
}

include ':app', ':DSIEMVAndroid'
rootProject.name='DSIEMVAndroidDemo'
